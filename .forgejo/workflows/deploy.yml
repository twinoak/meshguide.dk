name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest into /var/www/meshguide.dk/main
        run: git -C /var/www/meshguide.dk/main pull --ff-only
